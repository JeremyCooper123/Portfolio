import React, { useState, useEffect } from 'react';
import { ArrowRight, X, Play, Mail, Instagram, Twitter, Github, ChevronRight } from 'lucide-react';

// ============================================================================
// 1. DATA CONFIGURATION (EDIT THIS SECTION TO UPDATE YOUR PORTFOLIO)
// ============================================================================
const GOOGLE_SHEET_CSV_URL = ""; // 👈 PASTE YOUR PUBLISHED GOOGLE SHEET CSV URL HERE

const PORTFOLIO_DATA = {
  header: {
    name: "ALEX.MOTION",
    role: "Motion Designer & 3D Artist",
    availability: "Available for freelance",
  },
  hero: {
    title: "Bringing ideas to life through motion.",
    subtitle: "Specializing in 3D animation, kinetic typography, and immersive visual experiences for forward-thinking brands.",
    // A placeholder Lottie animation for the hero section
    lottieUrl: "https://lottie.host/5a0928a3-2287-4340-97f4-d5382343f733/2qXmY0rJ0c.json" 
  },
  about: {
    title: "About Me",
    description: "I'm a multidisciplinary motion designer with 5+ years of experience crafting visually striking animations. I blend technical precision with artistic experimentation to create work that demands attention. When I'm not keyframing, I'm probably experimenting with generative art or learning new tools.",
    tools: ["Cinema 4D", "After Effects", "Blender", "Octane Render", "Houdini", "Figma"]
  },
  contact: {
    email: "hello@example.com",
    socials: [
      { name: "Instagram", icon: "instagram", url: "#" },
      { name: "Twitter", icon: "twitter", url: "#" },
      { name: "GitHub", icon: "github", url: "#" }
    ]
  },
  projects: [
    {
      id: "neon-echoes",
      title: "Neon Echoes",
      category: "3D Animation / Concept",
      client: "Personal Project",
      year: "2023",
      coverImage: "https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?q=80&w=2564&auto=format&fit=crop",
      description: "An exploration of cyberpunk aesthetics and retro-futuristic lighting setups using Cinema 4D and Octane. This project focused on creating loops that feel both nostalgic and deeply futuristic.",
      tools: ["Cinema 4D", "Octane", "After Effects"],
      videoPlaceholder: "https://images.unsplash.com/photo-1550745165-9bc0b252726f?q=80&w=2000&auto=format&fit=crop"
    },
    {
      id: "kinetic-flow",
      title: "Kinetic Flow",
      category: "Brand Identity / Typography",
      client: "Studio Vertex",
      year: "2024",
      coverImage: "https://images.unsplash.com/photo-1558591710-4b4a1ae0f04d?q=80&w=2574&auto=format&fit=crop",
      description: "A complete motion identity package for a modern architecture firm. The animations rely entirely on kinetic typography and stark monochrome contrasts to convey structure and flow.",
      tools: ["After Effects", "Illustrator"],
      videoPlaceholder: "https://images.unsplash.com/photo-1618005191268-ea0b5a682121?q=80&w=2000&auto=format&fit=crop"
    },
    {
      id: "organic-forms",
      title: "Organic Forms",
      category: "Simulation",
      client: "EcoLabs",
      year: "2023",
      coverImage: "https://images.unsplash.com/photo-1614850523459-c2f4c699c52e?q=80&w=2564&auto=format&fit=crop",
      description: "Simulating soft-body dynamics and cloth tears to represent the fragility of natural ecosystems. Used as the opening sequence for the EcoLabs annual summit.",
      tools: ["Houdini", "Redshift", "Premiere"],
      videoPlaceholder: "https://images.unsplash.com/photo-1518640467707-6811f4a6ab73?q=80&w=2000&auto=format&fit=crop"
    },
    {
      id: "synthwave",
      title: "Synthwave Nights",
      category: "Music Video",
      client: "The Midnight",
      year: "2022",
      coverImage: "https://images.unsplash.com/photo-1563089145-599997674d42?q=80&w=2564&auto=format&fit=crop",
      description: "A fully animated music video blending 2D cel animation with 3D environments. A love letter to 80s arcade culture and endless neon highways.",
      tools: ["Blender", "Grease Pencil", "After Effects"],
      videoPlaceholder: "https://images.unsplash.com/photo-1604871000636-074fa5117945?q=80&w=2000&auto=format&fit=crop"
    }
  ]
};

// ============================================================================
// 2. COMPONENTS & APP LOGIC
// ============================================================================

const LottieElement = ({ src, className }) => {
  return (
    <div className={className}>
      <lottie-player
        src={src}
        background="transparent"
        speed="1"
        style={{ width: '100%', height: '100%' }}
        loop
        autoplay
      ></lottie-player>
    </div>
  );
};

export default function App() {
  const [activeProject, setActiveProject] = useState(null);
  const [isScrolled, setIsScrolled] = useState(false);
  const [projects, setProjects] = useState(PORTFOLIO_DATA.projects);
  const [isLoadingProjects, setIsLoadingProjects] = useState(!!GOOGLE_SHEET_CSV_URL);

  // Helper to convert Google Drive viewing links into direct image/video links
  const convertDriveLink = (url) => {
    if (!url || !url.includes('drive.google.com')) return url;
    const fileIdMatch = url.match(/\/d\/(.+?)\/|\?id=(.+?)(?:&|$)/);
    const fileId = fileIdMatch ? (fileIdMatch[1] || fileIdMatch[2]) : null;
    return fileId ? `https://drive.google.com/uc?export=view&id=${fileId}` : url;
  };

  // Simple CSV Parser
  const parseCSV = (str) => {
    const result = [];
    let row = [];
    let inQuotes = false;
    let cell = '';
    for (let i = 0; i < str.length; i++) {
      const char = str[i];
      if (char === '"' && str[i+1] === '"') {
        cell += '"'; i++;
      } else if (char === '"') {
        inQuotes = !inQuotes;
      } else if (char === ',' && !inQuotes) {
        row.push(cell); cell = '';
      } else if (char === '\n' && !inQuotes) {
        row.push(cell); result.push(row); row = []; cell = '';
      } else {
        cell += char;
      }
    }
    if (cell || row.length) { row.push(cell); result.push(row); }
    return result;
  };

  // Fetch from Google Sheets on load
  useEffect(() => {
    if (!GOOGLE_SHEET_CSV_URL) return;
    
    const fetchSheet = async () => {
      try {
        const response = await fetch(GOOGLE_SHEET_CSV_URL);
        const csvText = await response.text();
        const rows = parseCSV(csvText);
        
        // Ensure we have headers
        if (rows.length < 2) return;
        
        const headers = rows[0].map(h => h.trim().toLowerCase());
        
        const fetchedProjects = rows.slice(1).filter(row => row.length > 1 && row[0]).map((row, rowIndex) => {
          const project = {};
          headers.forEach((header, index) => {
            let val = row[index] ? row[index].trim() : '';
            
            // Map comma separated strings back to arrays
            if (header === 'tools') {
              project[header] = val.split(',').map(t => t.trim());
            } 
            // Convert drive links
            else if (header === 'coverimage' || header === 'videoplaceholder') {
              const keyName = header === 'coverimage' ? 'coverImage' : 'videoPlaceholder';
              project[keyName] = convertDriveLink(val);
            } 
            else {
              project[header] = val;
            }
          });
          
          // Provide fallback ID if missing
          if (!project.id) project.id = `project-${rowIndex}`;
          return project;
        });
        
        if (fetchedProjects.length > 0) {
          setProjects(fetchedProjects);
        }
      } catch (error) {
        console.error("Error loading projects from Google Sheet:", error);
      } finally {
        setIsLoadingProjects(false);
      }
    };
    
    fetchSheet();
  }, []);

  // Load Lottie Player Script dynamically
  useEffect(() => {
    if (!document.querySelector('script[src*="lottie-player"]')) {
      const script = document.createElement('script');
      script.src = 'https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js';
      script.async = true;
      document.body.appendChild(script);
    }

    const handleScroll = () => {
      setIsScrolled(window.scrollY > 50);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  // Prevent background scrolling when modal is open
  useEffect(() => {
    if (activeProject) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = 'unset';
    }
  }, [activeProject]);

  return (
    <div className="min-h-screen bg-zinc-950 text-zinc-50 font-sans selection:bg-lime-400 selection:text-zinc-950">
      
      {/* Background Grid Texture */}
      <div className="fixed inset-0 pointer-events-none opacity-[0.03] z-0" 
           style={{ backgroundImage: `linear-gradient(to right, #ffffff 1px, transparent 1px), linear-gradient(to bottom, #ffffff 1px, transparent 1px)`, backgroundSize: '40px 40px' }} />

      {/* Navigation */}
      <nav className={`fixed top-0 w-full z-40 transition-all duration-300 ${isScrolled ? 'bg-zinc-950/80 backdrop-blur-md py-4 border-b border-zinc-900' : 'py-6 bg-transparent'}`}>
        <div className="max-w-7xl mx-auto px-6 md:px-12 flex justify-between items-center">
          <div className="font-bold tracking-tighter text-xl">
            {PORTFOLIO_DATA.header.name}
          </div>
          <div className="flex items-center gap-6 text-sm font-medium text-zinc-400">
            <div className="hidden md:flex items-center gap-2">
              <span className="relative flex h-2 w-2">
                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-lime-400 opacity-75"></span>
                <span className="relative inline-flex rounded-full h-2 w-2 bg-lime-500"></span>
              </span>
              {PORTFOLIO_DATA.header.availability}
            </div>
            <a href="#work" className="hover:text-lime-400 transition-colors">Work</a>
            <a href="#about" className="hover:text-lime-400 transition-colors">About</a>
          </div>
        </div>
      </nav>

      {/* Main Content */}
      <main className="relative z-10">
        
        {/* Hero Section */}
        <section className="min-h-[90vh] flex flex-col justify-center max-w-7xl mx-auto px-6 md:px-12 pt-20">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div className="max-w-2xl">
              <p className="text-lime-400 font-medium tracking-wide mb-4 uppercase text-sm">{PORTFOLIO_DATA.header.role}</p>
              <h1 className="text-5xl md:text-7xl lg:text-8xl font-bold tracking-tighter leading-[1.05] mb-8 text-white">
                {PORTFOLIO_DATA.hero.title}
              </h1>
              <p className="text-lg md:text-xl text-zinc-400 max-w-lg mb-10 leading-relaxed">
                {PORTFOLIO_DATA.hero.subtitle}
              </p>
              <a href="#work" className="inline-flex items-center gap-3 bg-white text-zinc-950 px-8 py-4 rounded-full font-semibold hover:bg-lime-400 transition-colors duration-300 group">
                View Projects
                <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
              </a>
            </div>
            
            {/* Hero Lottie / Visual */}
            <div className="relative h-[400px] lg:h-[600px] w-full hidden md:block opacity-60 mix-blend-screen pointer-events-none">
              <LottieElement src={PORTFOLIO_DATA.hero.lottieUrl} className="absolute inset-0 scale-125" />
            </div>
          </div>
        </section>

        {/* Projects Grid Section */}
        <section id="work" className="py-24 max-w-7xl mx-auto px-6 md:px-12">
          <div className="flex justify-between items-end mb-16">
            <h2 className="text-4xl md:text-5xl font-bold tracking-tighter">Selected Work</h2>
            <span className="text-zinc-500 hidden md:block">({projects.length}) Projects</span>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12">
            {isLoadingProjects ? (
              <div className="col-span-full py-12 text-center text-zinc-500 animate-pulse">Loading projects...</div>
            ) : projects.map((project, index) => (
              <div 
                key={project.id} 
                onClick={() => setActiveProject(project)}
                className={`group cursor-pointer ${index % 2 !== 0 ? 'md:mt-24' : ''}`} // Staggered layout on desktop
              >
                {/* Image Container with Hover Effects */}
                <div className="relative overflow-hidden rounded-2xl aspect-[4/5] bg-zinc-900 mb-6">
                  <div className="absolute inset-0 bg-zinc-950/20 group-hover:bg-transparent transition-colors duration-500 z-10" />
                  <img 
                    src={project.coverImage} 
                    alt={project.title} 
                    className="object-cover w-full h-full transform group-hover:scale-105 transition-transform duration-700 ease-out opacity-80 group-hover:opacity-100"
                  />
                  {/* Hover "Play/View" Button */}
                  <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-500 z-20">
                    <div className="bg-lime-400 text-zinc-950 rounded-full p-4 transform translate-y-4 group-hover:translate-y-0 transition-transform duration-500">
                      <Play className="w-6 h-6 fill-current ml-1" />
                    </div>
                  </div>
                </div>
                
                {/* Project Meta */}
                <div className="flex justify-between items-start">
                  <div>
                    <h3 className="text-2xl font-bold mb-2 group-hover:text-lime-400 transition-colors">{project.title}</h3>
                    <p className="text-zinc-400 text-sm">{project.category}</p>
                  </div>
                  <span className="text-zinc-600 font-mono text-sm">{project.year}</span>
                </div>
              </div>
            ))}
          </div>
        </section>

        {/* About & Contact Section */}
        <section id="about" className="py-24 border-t border-zinc-900 bg-zinc-900/20">
          <div className="max-w-7xl mx-auto px-6 md:px-12 grid grid-cols-1 lg:grid-cols-2 gap-16">
            
            {/* About */}
            <div>
              <h2 className="text-3xl font-bold tracking-tighter mb-8">{PORTFOLIO_DATA.about.title}</h2>
              <p className="text-zinc-400 leading-relaxed text-lg mb-8">
                {PORTFOLIO_DATA.about.description}
              </p>
              <div className="flex flex-wrap gap-3">
                {PORTFOLIO_DATA.about.tools.map(tool => (
                  <span key={tool} className="px-4 py-2 rounded-full border border-zinc-800 text-sm text-zinc-300">
                    {tool}
                  </span>
                ))}
              </div>
            </div>

            {/* Contact */}
            <div className="lg:pl-16 lg:border-l border-zinc-900">
              <h2 className="text-3xl font-bold tracking-tighter mb-8">Let's create together.</h2>
              <a 
                href={`mailto:${PORTFOLIO_DATA.contact.email}`}
                className="inline-flex items-center gap-3 text-2xl md:text-4xl font-light hover:text-lime-400 transition-colors mb-12"
              >
                {PORTFOLIO_DATA.contact.email}
                <ArrowRight className="w-8 h-8" />
              </a>
              
              <div className="flex gap-6">
                {PORTFOLIO_DATA.contact.socials.map(social => {
                  const Icon = social.icon === 'instagram' ? Instagram : social.icon === 'twitter' ? Twitter : Github;
                  return (
                    <a key={social.name} href={social.url} className="p-3 rounded-full bg-zinc-900 hover:bg-lime-400 hover:text-zinc-950 transition-colors text-zinc-400">
                      <Icon className="w-5 h-5" />
                    </a>
                  )
                })}
              </div>
            </div>
          </div>
        </section>
        
        {/* Footer */}
        <footer className="py-8 text-center text-zinc-600 text-sm border-t border-zinc-900">
          <p>© {new Date().getFullYear()} {PORTFOLIO_DATA.header.name}. All rights reserved.</p>
        </footer>

      </main>

      {/* ============================================================================ */}
      {/* 3. PROJECT DETAIL MODAL (Takes over screen when project is clicked) */}
      {/* ============================================================================ */}
      
      <div 
        className={`fixed inset-0 z-50 bg-zinc-950 overflow-y-auto transition-transform duration-700 ease-[cubic-bezier(0.16,1,0.3,1)] ${activeProject ? 'translate-y-0' : 'translate-y-full'}`}
      >
        {activeProject && (
          <div className="min-h-screen pb-24">
            
            {/* Modal Header */}
            <div className="sticky top-0 w-full p-6 md:p-8 flex justify-between items-center z-50 mix-blend-difference text-white">
              <span className="font-bold tracking-tighter">{activeProject.title}</span>
              <button 
                onClick={() => setActiveProject(null)}
                className="p-3 bg-white/10 hover:bg-white/20 backdrop-blur-md rounded-full transition-colors group"
              >
                <X className="w-6 h-6 group-hover:rotate-90 transition-transform duration-300" />
              </button>
            </div>

            <div className="max-w-5xl mx-auto px-6 md:px-12 pt-12">
              
              {/* Project Hero / Video Placeholder */}
              <div className="relative aspect-video w-full bg-zinc-900 rounded-3xl overflow-hidden mb-16 group">
                <img 
                  src={activeProject.videoPlaceholder} 
                  alt={`${activeProject.title} video`}
                  className="w-full h-full object-cover opacity-70"
                />
                {/* Play Button Overlay */}
                <div className="absolute inset-0 flex items-center justify-center">
                  <div className="bg-lime-400/90 text-zinc-950 rounded-full p-6 cursor-pointer hover:scale-110 hover:bg-lime-400 transition-all duration-300">
                    <Play className="w-8 h-8 fill-current ml-1" />
                  </div>
                </div>
                <div className="absolute bottom-6 left-6 text-sm font-mono text-zinc-400">
                  // Click to play demo reel
                </div>
              </div>

              {/* Project Info Layout */}
              <div className="grid grid-cols-1 lg:grid-cols-3 gap-12">
                
                {/* Meta Data */}
                <div className="col-span-1 space-y-8">
                  <div>
                    <h4 className="text-zinc-500 text-sm uppercase tracking-wider mb-2">Client</h4>
                    <p className="text-lg">{activeProject.client}</p>
                  </div>
                  <div>
                    <h4 className="text-zinc-500 text-sm uppercase tracking-wider mb-2">Role / Category</h4>
                    <p className="text-lg">{activeProject.category}</p>
                  </div>
                  <div>
                    <h4 className="text-zinc-500 text-sm uppercase tracking-wider mb-2">Year</h4>
                    <p className="text-lg">{activeProject.year}</p>
                  </div>
                  <div>
                    <h4 className="text-zinc-500 text-sm uppercase tracking-wider mb-3">Tools</h4>
                    <div className="flex flex-wrap gap-2">
                      {activeProject.tools.map(tool => (
                        <span key={tool} className="px-3 py-1 bg-zinc-900 rounded-md text-sm text-zinc-300">
                          {tool}
                        </span>
                      ))}
                    </div>
                  </div>
                </div>

                {/* Description */}
                <div className="col-span-1 lg:col-span-2">
                  <h3 className="text-4xl font-bold tracking-tighter mb-8">The Process</h3>
                  <p className="text-xl text-zinc-300 leading-relaxed">
                    {activeProject.description}
                  </p>
                  
                  {/* Additional content placeholders - you can add more images/videos here */}
                  <div className="mt-12 grid grid-cols-2 gap-6">
                     <div className="aspect-square bg-zinc-900 rounded-2xl overflow-hidden">
                       <img src={activeProject.coverImage} className="w-full h-full object-cover opacity-60 hover:opacity-100 transition-opacity" alt="Process 1" />
                     </div>
                     <div className="aspect-square bg-zinc-900 rounded-2xl overflow-hidden">
                       <img src={activeProject.videoPlaceholder} className="w-full h-full object-cover opacity-60 hover:opacity-100 transition-opacity" alt="Process 2" />
                     </div>
                  </div>
                </div>

              </div>

              {/* Next Project Hint */}
              <div className="mt-32 pt-12 border-t border-zinc-900 flex justify-between items-center cursor-pointer group hover:text-lime-400 transition-colors" onClick={() => setActiveProject(null)}>
                <span className="text-xl font-bold tracking-tighter">Back to all projects</span>
                <ChevronRight className="w-8 h-8 group-hover:translate-x-2 transition-transform" />
              </div>

            </div>
          </div>
        )}
      </div>

    </div>
  );
}
